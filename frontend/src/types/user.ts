export interface User {
    displayName: string
    username: string
    email: string
    _id: string
    rankSalary: string
    role: string // Optional role field for future use
    createdAt: string
    updatedAt: string
}
